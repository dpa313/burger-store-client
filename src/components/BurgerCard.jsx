import React from "react";

const BurgerCard = ({burger}) => {
    const {quantity,supplier,taste,category,details,photoUrl} = burger
    console.log(object);
  return (
    <div></div>
  );
};

export default BurgerCard;
